const leia = require('readline-sync');

let palavra 1, palavra2, palavra3;

palavra1 = leia.question("Digite a primeira palavra")
palavra1 = leia.question("Digite a primeira palavra")
palavra1 = leia.question("Digite a primeira palavra")

if (palavra1 === "vertebrado") {
    if (palavra2 === "ave");
}
if (palavra3 === "Aguia"); {
            else
    console.log("Pomba");
} else {
    if (palavra3 === "herbivoro" {
        console.log("Vaca");
                    else {
            console.log("Homen");
        }else {
            if (palavra2 === "inseto") {
                if (palavra3 === "herbivoro") {
                    console.log("Lagarta");
                } else {
                    console.log("Pulga");
                }
            }


        }